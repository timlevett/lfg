# LFG hook config for OpenAI Codex CLI (with boopifier)
# Copy to ~/.codex/config.toml (or merge into existing config)
#
# Note: hooks are behind a feature flag — add this to enable them.
# The actual hook definitions go in ~/.codex/hooks.json (see codex-boopifier.json).
[features]
codex_hooks = true
